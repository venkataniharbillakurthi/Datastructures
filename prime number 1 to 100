/*program to printf prime number upto 100 */
#include<stdio.h>
void main()
{
	int i,j,num,count;
	printf("prime number between 1 to 100");
	for(i=1;i<=100;i++)
	{
		count=0;
		for(j=2;j<i;j++)
		{
			if(i%j==0)
			{
				count++;
				break;
			}
		}
		if(count==0)
		printf("\n %d",i);
	}
}
